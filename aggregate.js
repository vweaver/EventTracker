// aggregate.js — pure functions: bucket mapping + aggregation.
//
// Slice 1 stub; real implementation lands in Slice 3. Kept as a
// separate module so db.js and app.js never need to re-import later.
